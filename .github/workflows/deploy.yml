name: CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Build Step
      run: echo "Building application..."

    - name: Deploy Step
      run: echo "Deploying application successfully 🚀"